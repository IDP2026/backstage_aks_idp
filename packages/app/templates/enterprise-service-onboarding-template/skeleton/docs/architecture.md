# Architektur

## Kontext

Der Service `${{ values.serviceName }}` ist dem System `${{ values.system }}` zugeordnet.

## Zielarchitektur

[Architekturdiagramm hier]

## Technische Einordnung

Der Service ist für eine spätere containerisierte Ausführung vorbereitet. Die Kubernetes-Manifeste befinden sich im Verzeichnis `k8s/`.

## Abhängigkeiten

Aktuelle Abhängigkeiten müssen im Rahmen der fachlichen Implementierung ergänzt werden.
