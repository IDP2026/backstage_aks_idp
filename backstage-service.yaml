apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: idpapp
spec:
  type: ClusterIP
  selector:
    app: backstage
  ports:
    - protocol: TCP
      port: 7007
      targetPort: 7007
