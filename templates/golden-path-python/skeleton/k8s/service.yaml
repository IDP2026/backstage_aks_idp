apiVersion: v1
kind: Service
metadata:
  name: golden-path-python
  namespace: idpapp
spec:
  type: ClusterIP
  selector:
    app: golden-path-python
  ports:
    - name: http
      port: 80
      targetPort: 8080
